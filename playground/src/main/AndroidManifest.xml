<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="mx.yellowme.youst.playground">

    <dist:module
        dist:instant="false"
        dist:onDemand="false"
        dist:title="@string/title_playground">
        <dist:fusing dist:include="false" />
    </dist:module>

    <application>
        <activity
            android:name="mx.yellowme.youst.playground.PlaygroundActivity"
            android:theme="@style/SharedAppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>

        <activity
            android:name="mx.yellowme.youst.playground.navigation.NavigationActivity"
            android:label="Jetpack Navigation"
            android:parentActivityName="mx.yellowme.youst.playground.PlaygroundActivity"
            android:theme="@style/SharedAppTheme.NoActionBar.TransparentStatus">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mx.yellowme.youst.playground.PlaygroundActivity" />
        </activity>
    </application>

</manifest>

